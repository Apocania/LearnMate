[project]
name = "guochuang-api"
version = "0.1.0"
description = "FastAPI backend for Guochuang intelligent learning companion."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

