# LearnMate 项目审查报告

审查时间：2026-05-25
项目位置：`/home/apocania/LearnMate`
项目定位：面向师生教学场景的智能伴学系统平台
当前阶段：可演示的进阶原型，已经具备课程、互动、资料、智能检索问答、学习记录闭环、伴学师教学看板、演示数据脚本和儿童化展示 UI；生产级稳定性仍需继续打磨。

## 1. 总体结论

LearnMate 当前已经从基础教学平台原型推进到“智能伴学闭环”原型。

已经具备：

- React + TypeScript + Vite 前端。
- FastAPI + SQLAlchemy 后端。
- PostgreSQL、Redis、MinIO 的 Docker Compose 基础配置。
- 登录注册、Token 鉴权、学生/伴学师角色权限。
- 游客浏览、学生操作、伴学师管理的权限体系。
- 课程创建、编辑、删除、浏览，创建课程已拆成独立页面。
- 课程章节创建、编辑、删除、浏览。
- 学生加入课程、退出课程；课程作者可查看和移除学生名单。
- 草稿课程只对创建它的伴学师可见，学生和游客不会看到草稿课程状态或草稿内容。
- 课件上传、浏览、下载、删除，并可绑定课程和章节。
- 课件文本抽取、知识库 chunk 写入、本地 embedding + 关键词混合检索。
- 智能伴学聊天页面、课程选择、会话记录、引用来源和 OpenAI 兼容大模型配置入口。
- 讨论区 Markdown 发帖、附件、评论、点赞、评论删除。
- 讨论区分页、课程筛选、关键词搜索、伴学师隐藏/恢复/删除帖子；帖子列表不再展示附带标签。
- 消息中心，包含点赞提醒、评论提醒、私信和公告。
- 学习事件写入和个人中心；学生展示学习报告，伴学师展示课程建设、学生参与、章节资料和教学建议看板。
- 前端顶部导航、头像上传、消息未读角标和路由懒加载。
- 首页、课程、讨论、智能伴学和个人中心已按儿童友好风格增加更活泼的视觉层次、装饰元素、模块对齐和轻量动效。
- 演示数据脚本可生成学生、伴学师、课程、章节、课件、讨论、消息、智能伴学会话和头像数据，便于截图展示。
- Alembic 迁移骨架和初始 schema。
- 后端测试覆盖基础鉴权、消息、头像、报告、AI、章节和学习事件。

仍建议继续完善：

- pgvector 原生索引和外部 embedding 模型尚未接入，目前使用本地哈希 embedding。
- 智能伴学流式输出、历史会话列表、用户反馈和多轮上下文精细控制仍可继续增强。
- 图片 OCR 尚未接入；PDF 解析依赖 `pypdf`。
- MinIO 已可配置为课件对象存储，但头像和论坛附件仍使用本地目录。
- 测试覆盖仍可继续扩大到真实数据库集成测试、前端交互测试和端到端测试。
- 生产部署前仍需安全加固，例如 refresh token、登录失败限制、上传病毒扫描、审计日志等。

一句话判断：

> 当前项目适合课程设计展示、原型路演和继续产品化迭代；如果要上线给真实用户使用，还需要补真实部署演练、前端自动化测试、智能伴学流式体验、pgvector 检索和安全加固。

## 2. 本次验证结果

### 2.1 前端构建

命令：

```bash
npm run build --prefix apps/web
```

结果：通过。

说明：

- TypeScript 构建通过。
- Vite 打包通过。
- 页面继续按路由拆分 chunk。

### 2.2 后端语法检查

命令：

```bash
python3 -m compileall apps/api/app apps/api/migrations
```

结果：通过。

### 2.3 后端测试

命令：

```bash
cd apps/api
APP_ENV=test .venv/bin/python -m pytest tests
```

结果：

```text
17 passed
```

当前测试包括：

- 健康检查接口。
- 注册时非法用户名校验。
- 个人报告接口鉴权和响应结构。
- 头像上传鉴权和服务逻辑。
- 消息接口鉴权、未读数和学生发送私信权限校验。
- 智能伴学鉴权、会话 ID 和引用结构。
- 课程章节公开读取、创建鉴权和当前用户传递。
- 学习事件鉴权和写入结构。

### 2.4 Alembic 迁移验证

命令：

```bash
cd apps/api
APP_ENV=test .venv/bin/python -m alembic upgrade head --sql
```

结果：离线 SQL 生成通过。

说明：

- 已新增 `apps/api/alembic.ini`。
- 已新增 `apps/api/migrations/env.py`。
- 已新增初始迁移 `20260520_0001_initial_schema.py`。
- 在线 `alembic current` 需要真实数据库连接；当前环境数据库主机不可解析，因此未做在线迁移验证。

## 3. 当前核心能力

### 3.1 课程与章节

- 游客可浏览课程。
- 学生可加入或退出课程。
- 伴学师可通过独立创建页创建课程，并编辑、删除自己创建的课程。
- 草稿课程仅创建者可见，学生端不会展示发布/草稿状态。
- 课程详情页展示课程信息、章节目录、绑定课件和统计信息。
- 课程作者可创建、编辑、删除章节，向课程或章节上传课件，并管理课程学生名单。

### 3.2 文件与知识库

- 课件支持课程和章节归属。
- 文件元数据保存 `storage_provider`、`object_key`、`course_id`、`chapter_id` 等字段。
- 本机开发默认 `STORAGE_BACKEND=local`。
- Docker/部署场景可设置 `STORAGE_BACKEND=minio`。
- 上传文本、Markdown、DOCX 和 PDF 后会抽取文本并切分为知识库 chunk。
- 知识库 chunk 保存关键词和本地 embedding，用于智能伴学检索。

### 3.3 智能伴学

- `/assistant` 页面可选择课程资料并提问。
- `/api/assistant/messages` 需要登录用户，并允许学生和伴学师使用。
- 请求支持 `content`、`course_id`、`session_id`。
- 响应包含 `session_id`、`answer`、`citations`。
- `citations` 包含文档 ID、标题、切片序号、片段和来源 URL。
- 配置 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL` 后会调用 OpenAI 兼容接口。
- 未配置大模型时使用本地检索式回答，保证演示环境可用。
- 智能伴学问答会写入会话、消息和学习事件。

### 3.4 讨论交流

- 帖子列表支持分页。
- 支持按课程筛选。
- 支持关键词搜索标题和正文。
- 帖子列表不再展示课程/标签式附带信息，减少视觉干扰。
- 草稿课程关联帖子仅课程创建者可见。
- 伴学师可查看不同状态帖子，并隐藏或恢复帖子。
- 登录用户可发帖、评论、点赞。
- 点赞和评论会给帖子作者生成消息提醒。

### 3.5 学习记录与个人中心

- 选课、退课、创建章节、上传课件、发帖、评论、点赞、智能伴学提问等行为会写入 `learning_events`。
- `/api/learning-records` 可查看个人学习事件。
- `/api/reports/me` 按角色统计个人中心数据。学生侧展示课程、讨论、智能伴学问答、资料上传、估算学习投入、能力进度、学习轨迹和建议；伴学师侧展示建课数量、学生数量、章节数、课件数、课程概览、教学动态和教学建议。

## 4. 技术结构

核心目录：

```text
LearnMate/
├── apps/
│   ├── web/                  # React 前端
│   └── api/                  # FastAPI 后端
├── deploy/                   # Docker Compose、Nginx、部署说明
├── docs/                     # 设计文档
├── data/                     # 示例数据、知识库资料
├── packages/                 # 共享类型和工具
├── PROJECT_REVIEW.md
├── README.md
└── package.json
```

后端仍采用模块化单体结构，适合当前阶段快速迭代。AI、文件、学习分析等模块已经有相对清晰边界，后续可按负载和团队分工拆分服务。

## 5. 后续建议

建议优先级：

1. 接入真实 PostgreSQL/pgvector 迁移演练，把本地 embedding 字段升级为 pgvector 向量列。
2. 增加智能伴学流式输出、会话列表和历史消息查询。
3. 增加 Playwright 前端端到端测试，覆盖登录、选课、发帖、上传课件、智能伴学问答和伴学师教学看板。
4. 为文件上传增加病毒扫描、文件名审计、对象存储生命周期策略。
5. 增加 refresh token、登录失败限制、操作审计和更细粒度课程权限。
6. 继续增强课程封面、章节学习状态、我的课程视图，以及伴学师按课程查看学生学习进度、待回复讨论和课程健康度的能力。
